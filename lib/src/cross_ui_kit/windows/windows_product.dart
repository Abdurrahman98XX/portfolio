class WindowsButton extends CrossButton {
  const WindowsButton();
  @override
  Widget build() {
    return FilledButton(
      onPressed: () {},
      child: const Text('Filled Button'),
    );
  }
}
